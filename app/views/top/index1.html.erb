<% if false %>
<% flash.each do |key, value| %>
   <%= content_tag(:div, value, class: "alert-success") %>
<% end %>
<% end %>

<section class="main-header">
    <p class="flash"><%= notice %><%= alert %></p>
    <h1>Topical</h1>
    <p class="sub-title"><strong>a communicative field for new-topic freaks all over the world</strong>
</section>

$("#comments_area").html("<%= j(render 'comments/index', { comments: @comment.consultation.comments.order('id DESC'), consultation: @comment.consultation, event: @comment.consultation.event }) %>")
$('#comment_content').val('')
